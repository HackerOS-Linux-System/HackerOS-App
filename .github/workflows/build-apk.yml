name: Build Android APK

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: ./source-code
        run: |
          npm install
          # Instalujemy narzędzie do generowania ikon, jeśli nie ma go w projekcie
          npm install @capacitor/assets --save-dev

      - name: Build Web Assets
        working-directory: ./source-code
        run: npm run build

      - name: Generate Android Icons and Splash
        working-directory: ./source-code
        # To narzędzie automatycznie wygeneruje ikony w android/app/src/main/res/
        # Upewnij się, że masz plik assets/icon-only.png lub assets/icon.png
        run: npx capacitor-assets generate --android

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Add and Sync Android
        working-directory: ./source-code
        run: |
          npx cap add android || echo "Android already added"
          npx cap sync android

      - name: Build APK with Gradle
        working-directory: ./source-code/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hackeros-app-debug
          path: source-code/android/app/build/outputs/apk/debug/app-debug.apk
